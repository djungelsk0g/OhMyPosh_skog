if stage
    if work 
        "stage & work"
    else
        "stage"
    end
else
    if work
        "work"
    else 
        "the rest"
end
    
if stage and work
    "stage and work"
else 
    if stage and not work


{{if and .Arg1 .Arg2}} both complete. {{else}} incomplete. {{end}}

if upstream gone
    upstream
else
    if Working and staging
        working and staging
    else if working and not staging 
        working
    else if not working and staging 
        staging
    else
        if ahead and behind
            ahead and behind
        else if ahead and not behind 
            ahead
        else if not ahead and behind
            behind
        else 
            normal
        end
    end
end